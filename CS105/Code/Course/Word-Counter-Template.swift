import Foundation

func main() {
    // get the input from standard input using calls to readLine
    // convert the input String into a [String] of words
    // convert the [String] of words into a [String: Int] dictionary with words and frequencies
    // convert the dictionary into an array of (String, Int) pairs, sorted by decreasing value of the Int part.
    // print the values in the sorted array with the right format.
}

// main()
